import { defineConfig } from 'vite';
const PORT = parseInt(process.env.PORT) || 5173;

  export default defineConfig({
    server: {
      port: PORT,         // Use Replit-assigned PORT
      host: '0.0.0.0',    // Allow external access
    },
  });;